# Forçar LF per a tots els fitxers de text (evitar CRLF de Windows/VSCode)
* text=auto eol=lf

# Scripts bash — sempre LF
*.sh text eol=lf

# YAML i JSON — sempre LF
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
